import {SafeAreaView} from 'react-native';

import {Text} from '@components';

function App(): React.JSX.Element {
  return (
    <SafeAreaView>
      <Text>Ola</Text>
    </SafeAreaView>
  );
}

export default App;
